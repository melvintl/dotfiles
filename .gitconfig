[core]
    editor = nvim
	excludesfile = ~/.gitignore
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true
    side-by-side = true
    light = false
    line-numbers = true
    syntax-theme = zenburn

[merge]
    conflictstyle = zdiff3

[diff]
    algorithm = histogram

[pull]
    rebase = true

[push]
    # `git push` sets upstream on the first push of a new branch
    autoSetupRemote = true

[rebase]
    autosquash = true

[rerere]
    # remember conflict resolutions and replay them on rebase/merge
    enabled = true

[alias]
    co = checkout
    cob = checkout -b
    coo = !git fetch && git checkout
    br = branch
    d = diff --
    dft = -c diff.external=difft diff
    merged = branch --merged
    prune-branches = "!git branch -vv | grep ': gone]' | awk '{print $1}' | xargs -r git branch -d"
    s = status
    aa = add -A .
    cm = commit -m
    aacm = !git add -A . && git commit -m
    dev = !git checkout dev && git pull origin dev
    master = !git checkout master && git pull origin 
    main = !git checkout main && git pull origin 
    p = push
    ls = log --pretty=format:"%C(yellow)%h%Cred%d\\ %Creset%s%Cblue\\ [%cn]" --decorate
    ll = log --pretty=format:"%C(yellow)%h%Cred%d\\ %Creset%s%Cblue\\ [%cn]" --decorate --numstat
    lg = log --graph --oneline --decorate --all
    amend = commit --amend --no-edit

# Identity lives outside the repo; see .gitconfig.local.example
[include]
    path = ~/.gitconfig.local
