*.swp
.venv/
venv/
.envrc
.testmondata
coverage.xml
junit/
.coverage
htmlcov/
.aws-sam/
__pycache__/
.aider*

# Per-machine identity and secrets (not *.local: .tmux.conf.local is tracked)
.gitconfig.local
.gitconfig.work
.bashrc.local
.zshrc.local

# Claude Code per-machine permissions
**/.claude/settings.local.json

# Pi project-local permission rules
**/.pi/extensions/pi-permission-system/config.json
